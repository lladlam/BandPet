<template>
  <div class="page-container">
    <div class="page-header-container">
      <div class="page-header">
        <image src="../common/back.png" class="page-header-back-button" onclick="goBack"></image>
        <div class="header-title-time">
          <text class="page-time-display">{{ time }}</text>
          <text class="page-header-title">关于</text>
        </div>
      </div>
    </div>
    <div class="page-content">
      <div class="content">
        <text class="app-name">BANDPET</text>
        <text class="app-version">版本 0.3.3 Alpha</text>
        <div class="separator"></div>
        <text class="dev-title">参与开发的人员</text>
        <div class="dev-list">
          <text class="dev-name">@lladlam(代码)</text>
          <text class="dev-name">@Eric齐齐(UI设计)</text>
          <text class="dev-name">@御与魚余鱼(美术)</text>
          <text class="dev-name">@无源流沙(提供UI思路)</text>
        <text class="dev-title">我的处女作，只为了你。</text>
        <div class="dev-list">
          <text class="dev-name">0.3.3 Alpha: 更改账户登录</text>
          <text class="dev-name">由于Bug砸坏了一个键盘</text>
        </div>
      </div>
    </div>
  </div>
</div>
</template>

<style>
  .page-container {
    flex-direction: column;
    align-items: center;
    background-color: #000000;
    width: 100%;
    height: 100%;
  }
  .page-header-container {
    width: 100%;
    margin-bottom: 20px;
  }
  .page-time-display {
    color: #FFFFFF;
    font-size: 24px;
    margin-bottom: 2px;
  }
  .page-header {
    width: 100%;
    height: 90px;
    padding: 0 20px;
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
  }
  .page-header-back-button {
    width: 80px;
    height: 80px;
    margin-right: 10px;
  }
  .page-header-title {
    color: #FFFFFF;
    font-size: 32px;
  }
  .header-title-time {
    flex-direction: column;
    align-items: center;
  }
  .page-content {
    flex-direction: column;
    width: 100%;
    height: 100%;
    align-items: center;
  }
  .content { flex-direction: column; align-items: center; margin-top: 20px; }
  .app-name { color: #FFF; font-size: 45px; font-weight: bold; }
  .app-version { color: #AAA; font-size: 24px; margin-top: 10px; }
  .separator { width: 80%; height: 1px; background-color: #222; margin: 30px 0; }
  .dev-title { color: #AAA; font-size: 24px; margin-bottom: 20px; }
  .dev-list { flex-direction: column; align-items: center; }
  .dev-name { color: #FFF; font-size: 28px; margin-bottom: 10px; }
</style>

<script>
  import router from '@system.router';
  export default {
    data: {
      time: '00:00'
    },
    onInit() {
      this.updateTime();
      setInterval(this.updateTime, 5000);
    },
    updateTime() {
      const now = new Date();
      const hours = now.getHours().toString().padStart(2, '0');
      const minutes = now.getMinutes().toString().padStart(2, '0');
      this.time = `${hours}:${minutes}`;
    },
    goBack() {
      router.back();
    }
  }
</script>
